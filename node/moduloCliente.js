//esses dois são metodos para importar dados de um arquivo que está em outro modulo(cada modulo seria um arquivo diferente no armazenado no PC)
const moduloa = require('./moduloA')
const modulob = require('./modulob')

console.log(moduloa,modulob)
