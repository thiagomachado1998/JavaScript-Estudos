for (let contador=1;contador <=10;contador++){
    console.log(`contador = ${contador}`)
}

const nota = [7.4,7.8,8.2,10,2.7]

for (let i=1; i < nota.length;i++)
{
    console.log(`nota = ${nota[i]}`)/*para navegar no array com o for, crie uma variavel com valor do indice que voce quer iniciar e no console.log coloque esse variavel no indice do array, fazendo com que seja mostrado o indice de acordo com o valor da variavel*/
}

